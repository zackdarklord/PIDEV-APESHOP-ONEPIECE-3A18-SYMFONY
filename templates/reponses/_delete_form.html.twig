<form method="post" action="{{ path('app_reponses_delete', {'idreponse': reponse.idreponse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.idreponse) }}">
    <button class="btn">Delete</button>
</form>
