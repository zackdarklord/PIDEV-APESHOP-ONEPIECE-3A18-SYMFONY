{% extends 'base.html.twig' %}

{% block title %}Repavis{% endblock %}

{% block body %}
    <h1>Repavis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idrep</th>
                <td>{{ repavi.idrep }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ repavi.etat }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ repavi.reponse }}</td>
            </tr>
            <tr>
                <th>Dateajout</th>
                <td>{{ repavi.dateajout ? repavi.dateajout|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_repavis_index') }}">back to list</a>

    <a href="{{ path('app_repavis_edit', {'idrep': repavi.idrep}) }}">edit</a>

    {{ include('repavis/_delete_form.html.twig') }}
{% endblock %}
