<form method="post" action="{{ path('app_repavis_delete', {'idrep': repavi.idrep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repavi.idrep) }}">
    <button style="background-color: initial;font-weight: bolder;color: #0a58ca" class="btn btn-primary">Supprimer</button>
</form>
