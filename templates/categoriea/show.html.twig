{% extends 'base.html.twig' %}

{% block title %}Categoriea{% endblock %}

{% block body %}
    <h1>Categoriea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcategorie</th>
                <td>{{ categoriea.idcategorie }}</td>
            </tr>
            <tr>
                <th>Nomcategorie</th>
                <td>{{ categoriea.nomcategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categoriea_index') }}">back to list</a>

    <a href="{{ path('app_categoriea_edit', {'idcategorie': categoriea.idcategorie}) }}">edit</a>

    {{ include('categoriea/_delete_form.html.twig') }}
{% endblock %}
