<form method="post" action="{{ path('app_forums_delete', {'idforum': forum.idforum}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum.idforum) }}">
    <button class="btn">Delete</button>
</form>
