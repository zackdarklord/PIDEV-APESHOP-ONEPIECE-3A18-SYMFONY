<form method="post" action="{{ path('app_commandes_delete', {'numerocommande': commande.numerocommande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.numerocommande) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
