<form method="post" action="{{ path('app_produits_delete', {'numeroproduit': produit.numeroproduit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.numeroproduit) }}">
    <button class="btn">Delete</button>
</form>
