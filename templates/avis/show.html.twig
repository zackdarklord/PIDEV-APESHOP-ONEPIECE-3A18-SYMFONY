{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idavis</th>
                <td>{{ avi.idavis }}</td>
            </tr>
            <tr>
                <th>Contenuavis</th>
                <td>{{ avi.contenuavis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_index') }}">back to list</a>

    <a href="{{ path('app_avis_edit', {'idavis': avi.idavis}) }}">edit</a>

    {{ include('avis/_delete_form.html.twig') }}
{% endblock %}
