<form method="post" action="{{ path('app_factures_delete', {'numerofacture': facture.numerofacture}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.numerofacture) }}">
    <button class="btn">Delete</button>
</form>
